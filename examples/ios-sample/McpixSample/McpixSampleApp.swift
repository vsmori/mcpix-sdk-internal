// Entry point SwiftUI. Define o app e a Scene principal apontando
// para `ContentView` — onde o fluxo da SDK roda.

import SwiftUI

@main
struct McpixSampleApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
